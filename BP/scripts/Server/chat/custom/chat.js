// ini untuk custom prefix, range, color name & text, dimension, emoji dan debug
// Notes : tidak semua emoji support di minecraft bedrock (lihat dokumentasi di 
// https://wiki.bedrock.dev/text/emojis)
// untuk color role saya sarankan atur di function "const roleColorMap" saja
// default color name & text vanilla minecraft bedrock adalah "§f"

// Default Range
const DRank = ''

// Color Default name
const DColorN = '§f'

// Color Default text
const DColorT = '§f'

// Emoji the OverWolrd
const overworld = ''

// Emoji the Nether
const nether = ''

// Emoji the End
const end = ''

// Emoji default
const DEmoji = ''

// DeDugError
const DDim = ''

// Prefix Rank (untuk addon ini tidak terlalu di gunakan)
export { DRank, DColorN, DColorT, overworld, nether, end, DDim, DEmoji }